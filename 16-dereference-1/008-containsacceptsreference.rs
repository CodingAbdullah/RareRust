use std::collections::HashSet;

fn main() {
    // <Do Not Edit>
    let s = HashSet::from([1, 2, 3]);
    let e = 2;
    // </Do Not Edit>

    let result = s.contains(&e);

    println!("{}", result);
}
